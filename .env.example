# 数据库（docker-compose 中已通过 environment 覆盖，本地开发可改）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hetong

# JWT 密钥（生产环境务必修改）
JWT_SECRET=change-me-in-production

# 上传目录（后端容器内路径）
UPLOAD_DIR=/app/uploads
